/**
 * The Bible has many different Versions across different translations.
 * This class stores information to keep track of which is currently being used
 * @property {object} metadata data about the Version
 */
class Version {
  metadata: object;
  constructor() {}
}

export default Version;
