/**
 * The Bible has many different Versions across different translations.
 * @property {object} metadata data about the Version such as publisher or year
 */
class Version {
  metadata: object;
  constructor() {}
}

export default Version;
